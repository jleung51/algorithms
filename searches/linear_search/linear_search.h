#ifndef LINEAR_SEARCH_H
#define LINEAR_SEARCH_H

// Returns the index of the first appearance of TARGET, or -1 if TARGET is not found.
int linear_search( int* array, int len, const int TARGET );

#endif
